import 'dart:io';

void main() {
  List <dynamic> secastudents = [2, 4, 9, 8, 1, 5];
secastudents.removeAt(2);
  print(secastudents);


  // List <dynamic> secastudents = [2, 4, 9, 8, 1, 5];
  // secastudents.sort();
  // print(secastudents);
  // secastudents.replaceRange(secastudents.length - 1, secastudents.last, [4]);
  // print(secastudents);
}
